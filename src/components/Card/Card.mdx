import { Canvas, ArgTypes, PRIMARY_STORY } from '@storybook/addon-docs';
import { Primary } from './Card.stories';

# Card

<Canvas of={Primary} />

## Usage

### With an `icon`

- Utilize the `icon` prop by passing in an `IconName`

### With a `title`

- Utilize the `title` prop by passing in a `string`
- Utilize the `title` slot

## Props

<ArgTypes story={PRIMARY_STORY} />
