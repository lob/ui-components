import { Canvas, ArgTypes, PRIMARY_STORY } from '@storybook/addon-docs';
import { Primary } from './Label.stories';

# Lob Label

A label component for all input components to have consistent labels.

<Canvas of={Primary} />

## How to Use

The `label` prop populates the label text, and the `labelFor` the for="" attribute of the `<label>` element.

If the field is required, you can pass in the `required` prop to show the asterisk next to the label.

You can hide the label by setting the `srOnlyLabel` to true (it will still be read by a screen reader).

A tooltip can be added with the `tooltipContent` prop (String).
By default it will show on the left of the label ('leading'). If `tooltipPosition="trailing"` is passed, the tooltip will render on the right end of the label.

```html
<lob-label
  :label="label"
  :labelFor="labelFor"
  :required="required"
  :srOnlyLabel="srOnlyLabel"
/>
```

## Props

<ArgTypes story={PRIMARY_STORY} />
