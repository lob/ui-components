import { Canvas, ArgTypes, PRIMARY_STORY } from '@storybook/addon-docs';
import { Primary } from './Pagination.stories';

# Pagination

<Canvas of={Primary} />

## How to Use

- Pass in the `next` URL and the `previous` URL as props
- Listen for `@next`/ `@previous` events to handle navigation

## Props

<ArgTypes story={PRIMARY_STORY} />
